package com.thread;
/**
*this class cannot be access outside the package if make default
*because when we put activity in a separate folder it give error
*so make it public to access outside the package*/
public class Utils {
    public static final String TAG ="TAG";
}
